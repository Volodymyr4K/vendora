apiVersion: v1
kind: Service
metadata:
  name: vendora-web-canary
  namespace: vendora
spec:
  selector:
    app: vendora-web
    track: canary
  ports:
    - name: http
      port: 80
      targetPort: 3000
