apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vendora
resources:
  - ../../base
  - web-canary-service.yaml
patchesStrategicMerge:
  - web-canary.yaml
  - ingress-canary.yaml
