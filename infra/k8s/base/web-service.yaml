apiVersion: v1
kind: Service
metadata:
  name: vendora-web
  namespace: vendora
spec:
  selector:
    app: vendora-web
    track: stable
  ports:
    - name: http
      port: 80
      targetPort: 3000
