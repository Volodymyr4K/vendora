apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendora-redis
  namespace: vendora
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vendora-redis
  template:
    metadata:
      labels:
        app: vendora-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
